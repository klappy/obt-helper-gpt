[build]
  command = "npm run build"
  # Functions are bundled by SvelteKit adapter
  # The adapter automatically bundles ES modules to CommonJS
  functions = "netlify/functions"
  publish = "build"

[dev]
  command = "npm run dev"
  targetPort = 9173
  port = 9888
  autoLaunch = false